replicaCount: 2

image:
  repository: fahd81/flask-nginx
  tag: "9a43ab3b85776a05e6f06f4a52417661d9dc3cd2"
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 80
  targetPort: 5000

ingress:
  enabled: true
  className: nginx
  host: fahdtech.store
  tls:
    enabled: true
    secretName: flask-tls
    clusterIssuer: letsencrypt-production

resources:
  requests:
    cpu: "100m"
  limits:
    cpu: "300m"

env:
  FLASK_ENV: "production"
  DEBUG: "false"
  LOG_LEVEL: "info"
  DB_HOST: "fahd-postgres.postgres.database.azure.com"
  DB_NAME: "flaskdb"
  OTEL_SERVICE_NAME: "flask-app"
secret:
  POSTGRES_DB: sandbox
  POSTGRES_USER: flaskuser@fahd-postgres
  POSTGRES_PASSWORD: flaskpassword

autoscaling:
  enabled: false   # for now
  minReplicas: 1
  maxReplicas: 3
